# docs/

Documentation for things built in this workspace, written and filed automatically.

You should rarely need to open this folder directly. Document writes the files, `_index.md` routes to the right one, and prime loads the index so a session knows what exists without reading everything.

Retired docs move to `docs/archive/` rather than being deleted, so the history stays readable.
