# compliance

Staying on the right side of the line. Filled 2026-08-03 from Table 07, Operator Day.

**Taha El Harti** — `compliance-compass`: an engine that works out which compliance rules actually apply to a specific business by interviewing the owner, researching live where it must, and producing a ranked queue of what to do, what to skip, and what to take to a lawyer. Ships with a `lite` variant and a `regimes` knowledge base. `what-is-compliance-compass-workless.html` is the explainer.

The author's nested `.git` metadata was removed from this distribution so Git backups include the actual files instead of an empty repository pointer. The delivered revision was `0da4e8e95b5595dd925068684a22b8aa7b98e430`; no remote address was stored. The source files remain intact.

Read `START-HERE.md` first: it is addressed to Claude, not to you.

Nothing here is installed. Review `../COMPATIBILITY.md` first. Put an adapted skill in `.claude/skills/`, then run `python scripts/sync_harness_skills.py` from the workspace root so Claude and Codex receive the same copy.
