# Worked Example

Real output from a real run. Offer: AI that texts every lead in 60 seconds. ICP: real estate agents.

This is here so you know what good looks like before you run it yourself.

**Do not copy this content.** It is for one specific offer and one specific ICP, and every reel in it is already taken. Run the skills on your own offer and your own buyer.

- `icp.md` is what `ig-icp-research` produces on Day 1
- `launch-plan.md` is what `ig-ideas` produces on Day 3
- `scripts/` is what `ig-script` produces per reel

Every link in these files was verified before it was written.
