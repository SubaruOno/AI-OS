# てくてくまち TestFlightフィードバック(2026-09-17)

2026-09-17 の朝に新しいフィードバックが3件届いた。App Store Connect API で全件取得(累計6件)。クラッシュ報告はゼロ。画面写真は `~/Pictures/ai/20260917-1050-walktown-feedback-same.jpg`、`~/Pictures/ai/20260917-1051-walktown-feedback-tray-drag.jpg`、`~/Pictures/ai/20260917-1052-walktown-feedback-grid.jpg`。アプリの現状は [docs/walk-town.md](../../docs/walk-town.md)。

## 新しく届いた3件

| 時刻 | 送り主 | 内容 |
|---|---|---|
| 10:50 | rick@terakoya.ai | どちらも一緒では？ |
| 10:51 | subaru.ono@icloud.com | 下のラインアップのところからドラッグで島に置けるようにしたい |
| 10:52 | rick@terakoya.ai | ドラッグの時にマス目がみたい |

3件とも iPhone 17 / iOS 27 の縦画面。画面写真から読み取れること:

- 10:50 のおみやげ画面は「小さな本の箱」を開けたところで、ボタンは「つぎの包みへ」と「これを飾る」。包みはまだ残っている状態。
- 10:51 の島の画面では、トレイの先頭に本の箱が ×2 で並んでいる。ほかに舟、自転車、ピクニックテーブル。同時に4つへ黄色い点が付いている。
- 10:52 の島の画面では、ピクニックテーブルを動かしている最中で、置き先に緑のひし形が1つ出ている。まわりの小物は半透明。

## 前回(9/15)の3件

すべて 2026-09-16 に直した。包みの連打と一括開封、絵を触れる当たり判定、街の名前の保存、バウンスを控えめに、島を丸く、小物ごとの大きさの比率、小物を増やす。

## 直し方の提案

### 1. 動かしているあいだ、マス目を出す (10:52)

いま長押しして小物を動かすと、置き先に小さなひし形が1つ出るだけ。狙っているマスが分かりにくい。トレイから置くときはすでにマス目が光っているのに、動かすときだけ出ていない。

動かし始めたら置き待ちと同じマス目の光り方を出し、いま狙っているマスは白より強い色で塗る。仕掛けは `components/TownGrid.tsx` の `EmptyTileGlow` をそのまま使える。小さめの変更。

### 2. トレイから直接ドラッグで置く (10:51)

いまはトレイの小物をタップしてから島のマスをタップする2手。これを、トレイの小物を押したまま島へ運び、離したマスに置けるようにする。運んでいるあいだは小物が指についてきて、島のマス目が出る。

トレイは島の外にあるので、指の画面での位置から置き先のマスを割り出す処理を新しく足す。島の画面での位置を測って渡せば、マスの計算は `TownGrid` のものを共有できる。中くらいの変更。タップして置く今のやり方も残す。

### 3. 「どちらも一緒では？」(10:50) を確認した

シミュレーターに実際に画面を出して確かめた。

- 包みをまとめて開けたあとの「まとめて開けました」画面のボタンは「まちに飾りにいく」の1つだけ。2つは出ない。証拠は `~/Pictures/ai/20260917-1205-walktown-verify-bulk.png`。
- 包みがまだ残っているときの開封後の画面は「つぎの包みへ」と「これを飾る」で、動きは別(次へ進む / いま置く)。
- 包みが最後の1つのときの開封後の画面は「まちに飾りにいく」と「これを飾る」になり、この2つは同じ処理を呼ぶ。どちらを押しても置く準備をして町に戻る。証拠は `~/Pictures/ai/20260917-1205-walktown-verify-last-gift.png`。
- 包みが1つだけのときの画面には「まとめて開ける」がなく、開封前のボタンも出ない。証拠は `~/Pictures/ai/20260917-1205-walktown-verify-wrapped.png`。

同じ意味のボタンが2つ出るのは本当で、まとめて開けた画面ではなく、最後の包みを開けたときに起きる。場所は `app/open.tsx` の194〜199行目。上のボタンは `next()` を呼び、残りの包みが0だと中で `decorate()` に落ちる。下のボタンは直接 `decorate()` を呼ぶ。表示名の判定は `waiting > 1`、動きの判定は開いていない包みの数で、最後の1つでは両方が同じ結果になる。

直し方は、包みが残り1つのときは下の「これを飾る」を出さず、上のボタン1つにまとめる。表示名と動きの判定を同じ情報にそろえる。

### 4. ついでに見つけたこと

まとめて開けた画面で、同じ小物が2つ出た(木の案内板 ×2)。抽選は未所持を優遇するだけで、同じ物が続けて出ることを防いではいない。rickの写真の本の箱 ×2 も同じことが起きている可能性がある。

## 進め方

1と3を先に直し、2は次の山として取っておくのが順当。どちらも小さめなので、一度に終わる。

## 確認で触ったもの

確認のためシミュレーター(iPhone 17 Pro Max)のアプリDBを書き換えた。包み7個(未開封)・持ち物0・配置0・metaは元の4項目に戻してある。eventsテーブルの中身は確認の途中で消えた(開発用の記録なので影響はない)。
